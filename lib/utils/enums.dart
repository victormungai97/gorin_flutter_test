/// List of flavors the application holds
enum Environment {
  local,
  development,
  staging,
  production,
  unspecified,
}

/// Modes by which a user enters into the app
enum Authentication {
  LOGIN,

  REGISTRATION,
}
